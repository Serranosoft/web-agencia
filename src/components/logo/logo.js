import * as React from "react"

const Logo = () => (
    <img 
        src="/logo.png" 
        alt="tuwebencadiz.es" 
        style={{ display: 'block', maxWidth: '100%', height: 'auto' }}
    />
)

export default Logo
